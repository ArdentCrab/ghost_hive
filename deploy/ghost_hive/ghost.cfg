# Ghost:Hive v2 live — SPEC-v2. Host orchestration only (no kernel changes).
# Copy to stick root as ghost.cfg

[kernel]
ip = 10.17.47.1
udp = 17471
ssid = GHSTHIVE
replay_window = 64
ack_budget = 8

[ibss]
# Nominal plan (relay learns real LAN addrs dynamically)
worker = 10.17.47.2
relay = 10.17.47.3
peer_range = 10.17.47.10-30

[wire]
telem_min_gap_sec = 1
telem_host_hz = 2
hb_interval_sec = 30

[paths]
wsl_tmp = /tmp/ghost_hive
bind_file = peer.bind
log_dir = logs

[peers]
worker = W
relay =
phone = P
nas = N
router = R
family = F
mines = X,ML,MP,MN

[manager]
refresh_hz = 20
