# live/

PSP-EBOOT für Ghost:Hive v2 (`[GHv2]` Watch).

```bash
make eboot
make ghost-hive    # kopiert EBOOT.PBP hierher
```

Auf PSP: `PSP/GAME/GHOST/EBOOT.PBP` oder Stick `live/EBOOT.PBP`.

EBOOT.PBP wird nicht ins Git committed (Build-Artefakt).
